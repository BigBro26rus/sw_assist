generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Character {
  id        String   @id @default(cuid())
  uuid      String   @unique
  name      String   @default("Безымянный")
  data      Json
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
